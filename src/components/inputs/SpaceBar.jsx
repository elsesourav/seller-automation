const SpaceBar = ({
   className = "",
   width = "w-full",
}) => {
   return (
      <div
         className={`relative h-full ${width} ${className}`}
      >   
      </div>
   );
};

export default SpaceBar;
